{
  // Root tsconfig — только для IDE навигации по всему монорепо.
  // Каждый пакет компилируется своим собственным tsconfig.json.
  "files": [],
  "references": [
    { "path": "packages/config" },
    { "path": "packages/ui" }
  ]
}
